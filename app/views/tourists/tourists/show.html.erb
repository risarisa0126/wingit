<div class="container-fluid">
 	<div class="row">
		<div class="col">
				<%= attachment_image_tag @tourist, :tourist_profile_image, :fill, 1000, 1000, size: '150x150', class: "rounded-circle"  %>
				<%= @tourist.tourist_username %>
				<%= @tourist.tourist_age %>
				<%= @tourist.tourist_about_me %>
				<p>チャットする　→ チャットルーム に行く</p>
				<p>他のtouristもランダムで５人ほど表示する？</p>


				<% if @room.exists? %>
      		<%= link_to "チャットへ", room_path(current_guide.rooms.find_by(tourist_id: @tourist.id)) %>
    		<% else %>
          <%= form_for [:tourist, @newroom], url: rooms_path do |f| %>
          	<%= f.hidden_field :tourist_id, :value => @tourist.id %>
            <%= button_tag( class: "btn btn-primary btn-block") do %>
              <%= content_tag :span, "　チャットする", class: "glyphicon glyphicon-pencil" %>
            <% end %>
          <% end %>
        <% end %>
	 	</div>
	</div>
</div>
